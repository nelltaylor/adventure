<div id="submit-caption">
<%= form_for :comment, :url => {:action => 'create'}, remote: true do |f| %>
<%= f.text_area(:text, size: '60x4', maxlength: 140)  %>
 <%= f.submit 'Submit' %>
 <% end %>
</div>

